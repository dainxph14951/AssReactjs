import React from 'react';
import {createAsyncThunk , createSlice} from '@reduxjs/toolkit';
import { listProduct, addProduct,removeProduct,updateProduct } from '../../api/order';
const orderSlice = () => {
  return (
    <div>orderSlice</div>
  )
}

export default orderSlice